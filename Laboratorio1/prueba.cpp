// Archivo de prueba
/* Esto es un comentario de bloque
que debe ser ignorado por el lexer */

int main() {
    // Probando números
    int entero = 42;
    float pi = 3.14159;
    double ciencia = 1.6e-19;
    int hex = 0xFF;

    // Probando strings
    string saludo = "Hola \"Mundo\"";

    // Probando operadores
    if (entero >= 10 && hex != 0) {
        entero = entero + 1;
    }
}